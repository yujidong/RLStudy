# 写作风格指南（WRITING_STYLE）

> 写给本教材的作者/维护者（包括未来的 AI 协作者）。
> 参考了 Datawhale [Hello-Agents](https://github.com/datawhalechina/Hello-Agents) 的优秀实践，
> 结合本项目「notebook 可交互」的独有优势整理而成。
> 修改任何章节内容（`build_notebooks.py` 内联章 / `build_chXX.py` / `chXX_content.txt`）前请先读一遍。

## 八条核心技法

### 1. 问题链叙事：每一节都从「上一个概念解决不了的事」开场

读者翻开一节的第一句话，就应该知道「为什么要讲这个」。最有效的钩子是**上一节的局限引出一个关键问题**，本节来回答它。

❌ 改前：
> ## 2.1 从 bandit 到 MDP：引入「状态」
> 多臂老虎机只有一种情况……下面我们引入马尔可夫决策过程。

✅ 改后：
> ## 2.1 从 bandit 到 MDP：引入「状态」
> Ch01 的老虎机已经能平衡探索与利用了，但它有一个隐含的假设被我们悄悄绕过了：**拉哪根拉杆，不会改变明天有哪些拉杆**。下围棋可不行——这一步落在哪里，直接决定了棋盘接下来长什么样。当**动作开始改变世界**、而且**好的标准要看长远**时，我们需要一种新的语言来描述它。这就是 MDP。

### 2. 具体可感知的例子先于抽象定义

先给一个读者能在脑中「看见」的场景，再抽象成符号。判断标准：**读者能否向朋友复述这个例子**？

❌ 「策略梯度定义为 ∇J(θ) = E[…]」（先公式）
✅ 「想象你在学开枪打靶：打偏了就往反方向调一点，打中了就顺着这个姿势加练——『增大做出好动作的概率』，这就是全部直觉。形式化之后，就是策略梯度定理。」

### 3. 拟人化设问：让算法「自己说话」

把设计动机写成算法在自问的问题，比陈述句更有代入感：

- TD(0) 在问：「**我刚拿到的这个奖励，应该让我对眼前状态改变多少看法？**」
- PPO-clip 在问：「**这个新策略比采样时好多少？好得太多就别信——离样本太远了。**」

### 4. 「先猜再跑」：notebook 独有的交互优势

Hello-Agents 是静态书，做不到这个——**我们有 kernel**。在关键实验/验证 cell 之前，插入一个预测 cell：

```markdown
> 🤔 **先猜再跑**：运行下面 cell 之前，先写下你的预测——
> 贪心策略 18 步到目标，它的**净奖励**是多少？
> <details><summary>写下猜测后点开对照直觉</summary>
> 提示：路上有没有什么会扣分的东西？（再看一遍地图）
> </details>
```

猜错了的学习效果远好于直接看答案（预测误差驱动记忆）。

### 5. 真实世界的锚点

每个 toy 实验至少连一个真实世界的事实/数据集/论文结论。例：讲 GRPO 的 reward 时，用一道 GSM8K 真题（Janet 的鸭子）展示 DeepSeek-R1 的 reward 就是「答案对不对」；讲 Deadly Triad 时引用 DQN 论文里 Atari 的真实崩塌案例。

### 6. 节奏感总结：用一段排比收束旅程

章末小结除了表格，给一段有呼吸感的回顾（Hello-Agents 式）：

> 从老虎机的「眼前奖励」，到 MDP 的「未来折扣」；从 DP 的「全知全能」，到 TD 的「摸着石头过河」；从表格的一格一格，到神经网络的函数逼近——你现在站的位置，正好能看清下一条大河的走向。

### 7. 把读者放进决策者位置的思考题

章末练习不要只有「跑实验画曲线」；至少一道开放式设计题，让读者扮演决策者：

> 你是算法负责人，训练预算只够跑一个：带 KL 惩罚但慢 30% 的 PPO，还是快但会 reward hacking 的裸 RM 优化？写下你的选择和**你愿意承担的风险**。

### 8. 术语三件套：加粗 + 英文 + 一句话解释

术语首次出现：**中文加粗（English, 缩写）**，紧跟一句话人话解释。之后可以直接用缩写。

---

## 技术约定（沿用现有规范）

- 数学：关键推导放 `<details>` 折叠块；主线只放结论 + 直觉
- 每个「推导出的结论」都要有数值验证 cell（有限差分 / MC 采样对照）
- 引用：正文 `<sup>[n]</sup>`，章末列参考文献（bib 条目在 `README.md` 参考资料节）
- 交叉引用必须指向**真实存在**的小节号（改小节号时跑引用审计）
- 章末结构：小结 → 📝 练习 → 指向 `STUDY_GUIDE.md` 自测题

## 自查清单（改完一章后过一遍）

- [ ] 每一节的第一段说清了「为什么要讲这个」（问题链）
- [ ] 每个新概念先有具体例子/类比，再上符号
- [ ] 至少一个「先猜再跑」cell
- [ ] 至少一个真实世界锚点（数据集/论文/工业实践）
- [ ] 小结有一段排比式回顾 + 练习至少含一道开放设计题
- [ ] 交叉引用、术语三件套符合约定
